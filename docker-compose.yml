version: '3.8'

services:
  web:
    build: .
    container_name: snapsave
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - FLASK_DEBUG=0
      - DOWNLOAD_DIR=/tmp/snapsave
    volumes:
      - downloads:/tmp/snapsave
    restart: unless-stopped

volumes:
  downloads:
